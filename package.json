{
  "name": "softwarevala-management-system",
  "version": "1.0.0",
  "description": "Comprehensive management system for Software Vala Liberia",
  "scripts": {
    "dev": "bash START_BOTH_SERVERS.sh",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm run build",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "softwarevala",
    "software vala liberia",
    "management",
    "system"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
